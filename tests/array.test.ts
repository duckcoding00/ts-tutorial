import { describe, expect, it } from 'bun:test'

describe('Arrays', function() {
    it('Log Array', function() {
        const names :string[] = ['joko', 'budi', 'rehan']
        console.log(names)
    })
})